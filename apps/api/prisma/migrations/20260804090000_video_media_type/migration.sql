-- Картинки хранятся в той же таблице, что и ролики: у них общий путь публикации,
-- ленты и медиамаршрута. Отличать одно от другого нужно явным полем.
ALTER TABLE "Video" ADD COLUMN "mediaType" TEXT NOT NULL DEFAULT 'VIDEO';
