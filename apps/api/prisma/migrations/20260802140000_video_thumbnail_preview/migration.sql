-- Уменьшенное превью для плиток ленты хранится как отдельный объект.
-- Раньше оно создавалось на лету в медиамаршруте, из-за чего его нельзя было
-- отдать прямой ссылкой из объектного хранилища мимо сервера приложения.
ALTER TABLE "Video" ADD COLUMN "thumbnailPreviewStorageKey" TEXT;
