-- Облегчённая копия ролика для ленты (480p, без звука).
-- Колонка nullable, поэтому существующие видео не затрагиваются:
-- фоновый воркер сам догонит их и проставит ключ.
ALTER TABLE "Video" ADD COLUMN "previewStorageKey" TEXT;
